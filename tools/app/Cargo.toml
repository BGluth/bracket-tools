[package]
name = "bracket-tools-app"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
bracket-tools-scheduler-core = { path = "../scheduler/core" }
dioxus = { workspace = true }
tokio = { workspace = true, features = ["sync"] }

[features]
default = ["web"]
web = ["dioxus/web"]
desktop = ["dioxus/desktop"]
